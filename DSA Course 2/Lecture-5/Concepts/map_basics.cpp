/*
Map is a sequence of(key,value) pair, an associative array that stores a value for each key;

map is a associative container ;

header file : <map>

syntax: map < key_type , value_type > map_name{{key,value},{key,value}};


e.g. Phone["Shruti"]=123456;

here, Phone is map object,
      "Shruti" is key,
       123456 is value;


       (*iterator_name).first---> reference to key
       (*iterator_name).second-->reference to value

*/